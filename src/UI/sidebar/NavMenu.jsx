import React from 'react';
import styled from 'styled-components';

const StyledNavMenu = styled.nav`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.8rem;
`;

const NavMenu = () => {
  return <StyledNavMenu>NavMenu</StyledNavMenu>;
};

export default NavMenu;
